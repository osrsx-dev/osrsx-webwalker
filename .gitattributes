# Keep the Gradle wrapper script LF so it stays executable on Linux CI (a CRLF
# shebang makes `./gradlew` fail with "required file not found").
gradlew text eol=lf
*.sh text eol=lf
gradlew.bat text eol=crlf
*.jar binary
